# Backend runtime dependencies (Docker image).
# Excludes Dagster, dbt, MLflow, Copernicus, Kaggle, fpdf2, graphviz,
# rasterio, geopandas — none of which the running API needs.
#
# Keep in sync with pyproject.toml. Versions are pinned to >= match
# pyproject; uv resolves the actual lockfile-pinned versions.

# ── Core data ──────────────────────────────────────
numpy>=2.2.0
pandas>=2.3.0
pyarrow>=23.0.1
h3>=4.4.2
scipy>=1.17.1
pyyaml>=6.0

# ── DB ─────────────────────────────────────────────
psycopg2-binary>=2.9.11

# ── HTTP ───────────────────────────────────────────
httpx>=0.28.1
requests>=2.32.0

# ── API framework ──────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
bcrypt>=5.0.0
pyjwt>=2.11.0
email-validator>=2.3.0
slowapi>=0.1.9

# ── ML inference (audio + photo classifiers) ───────
# torch CPU wheels are pulled via the --extra-index-url in the
# Dockerfile so we don't ship 2GB of CUDA libraries to the VM.
torch>=2.10.0
torchvision>=0.25.0
xgboost>=3.2.0
scikit-learn>=1.8.0
timm>=1.0.25
albumentations>=2.0.8
librosa>=0.11.0
soundfile>=0.13.1
pillow>=11.0.0
